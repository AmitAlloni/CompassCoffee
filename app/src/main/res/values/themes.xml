<resources>
    <!-- Base application theme -->
    <style name="Theme.CoffeeCompass" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <item name="colorPrimary">@color/coffee_brown_light</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!-- Splash theme -->
    <style name="Theme.CoffeeCompass.Splash" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/splashBackgroundColor</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- Active item text appearance -->
    <style name="TextAppearance.BottomNavigation.Active" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">@color/coffee_brown_dark</item>
    </style>

    <!-- Inactive item text appearance -->
    <style name="TextAppearance.BottomNavigation.Inactive" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textColor">@color/coffee_brown</item>
    </style>

    <!-- Bottom Navigation View Style -->
    <style name="AppBottomNavigationViewStyle" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="itemIconTint">@color/selector_bottom_nav_color</item>
        <item name="itemTextColor">@color/selector_bottom_nav_color</item>
        <item name="backgroundTint">@color/coffee_brown_light</item>
        <item name="itemTextAppearanceActive">@style/TextAppearance.BottomNavigation.Active</item>
        <item name="itemTextAppearanceInactive">@style/TextAppearance.BottomNavigation.Inactive</item>
    </style>
</resources>
