<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".activity.ReviewsActivity">

    <!-- Top bar with search and sorting -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <AutoCompleteTextView
            android:id="@+id/searchCoffeeNameTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Search by Coffee Name"
            android:inputType="text" />

        <AutoCompleteTextView
            android:id="@+id/searchFlavorTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Search by Flavor"
            android:inputType="text" />

        <AutoCompleteTextView
            android:id="@+id/searchWriterTextView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Search by Writer"
            android:inputType="text" />

        <Button
            android:id="@+id/sortByRatingButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sort by Rating" />
    </LinearLayout>

    <!-- RecyclerView for Reviews -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewReviews"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/topBar"
        android:layout_above="@id/bottomNavigationView"
        android:layout_marginTop="16dp" />

    <!-- Bottom Navigation Bar -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigationView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="?android:attr/windowBackground"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/selector_bottom_nav_color"
        app:itemTextColor="@color/selector_bottom_nav_color"
        app:itemTextAppearanceActive="@style/TextAppearance.BottomNavigation.Active"
        app:itemTextAppearanceInactive="@style/TextAppearance.BottomNavigation.Inactive"
        app:labelVisibilityMode="labeled" />

</RelativeLayout>
